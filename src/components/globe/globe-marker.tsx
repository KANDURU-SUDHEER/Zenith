"use client";

interface GlobeMarkerProps {
  position: { latitude: number; longitude: number } | null;
}

/**
 * Displays an animated location info overlay when a point is selected on the globe.
 * NOTE: Disabled — location info is shown in the left sidebar.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function GlobeMarker(_props: GlobeMarkerProps) {
  return null;
}
